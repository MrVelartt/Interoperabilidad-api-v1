services:
  api:
    build: .
    container_name: fastapi-service
    ports:
      - "8000:8000"
    env_file: .env
    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: redis-service
    ports:
      - "6379:6379"
